{
  "name": "lighting-ocr-csv-read-fix-v2",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp sample_data.csv dist/sample_data.csv"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}